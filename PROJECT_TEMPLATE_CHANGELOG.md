# graph-project-template の変更履歴

**この層だけの履歴。** 履歴は 3 つに分かれており、**それぞれ持ち主が違う。**

| ファイル | 誰の履歴か | 派生への流れ方 |
| --- | --- | --- |
| [TEMPLATE_CHANGELOG.md](TEMPLATE_CHANGELOG.md) | `graph-doc-template`（検証ツール） | 下流すべてに流れる |
| **このファイル** | `graph-project-template`（統合型の層） | その派生に流れる |
| [PROJECT_CHANGELOG.md](PROJECT_CHANGELOG.md) | **そのプロジェクト自身** | **流れない**（`merge=ours`） |

**派生プロジェクトはこのファイルを書き換えない。** 自分の履歴は
`PROJECT_CHANGELOG.md` に書く。

現在の版: **0.10.3**

---

## 0.10.3 — 2026-09-23

### 修正

**META-05 の「`--strict` が効くのは main への push だけ」を直した。** 上流の 1.22.0 で、
PR でも `--strict` が走るようになった（setopic/graph-doc-template#6）。
PR で落ちなければ、マージした後の main も落ちない。

### 取り込む側の作業

**`docs/00-meta/dev-flow.md` は `merge=ours` だが、派生の側で書き換えていなければそのまま入る。**
`merge=ours` が効くのは両側が変わったときだけで、派生の側が変わっていなければ、
git はテンプレート側をそのまま取る（tournament-bot・medieval-idle・gacha-monitor はこれで入った）。
**書き換えている派生では入らない。** 取り込んだ後に 6 節の最後の段落を確かめ、
「main への push だけ」のままなら手で直す。

---

## 0.10.2 — 2026-09-23

### 修正

**`check_refs.py` が、文の途中の「なし」で照合を飛ばしていた**（setopic/graph-project-template#5）。

題と本文のどこかに「なし」を含めば照合を飛ばしていたので、「問題なし」「更新なし」「みなし」
「あり / なし」でも飛んだ。**`check` と `sync --check` の出力そのものが「問題なし」「更新なし」**
なので、検査結果を本文に貼ると必ず当たる。

| | 派生の PR | 照合が飛んだ | うち意図したもの |
| --- | --- | --- | --- |
| 直す前 | 30 件 | 10 件 | 1 件（tournament-bot #29 の「ノード: なし」） |
| 直した後 | 32 件 | 1 件 | 1 件 |

**意図しない 9 件は、id を書き間違えていても CI が緑になる状態だった。**
当て直すと、書いた id はすべて実在した（新しく落ちる PR は 0 件）。

**「なし」と読むのは、行の頭に書いたものだけにした**（`なし` / `ノード: なし` で始まる行）。
HTML コメント（PR テンプレートの記入案内）は読まない。**題は引き続き id を探す対象にする**
（コミットの 1 行目と同じ形で id を書く慣習がある）。判定を関数に分け、テストを足した（`tests/test_check_refs.py`）。

### 却下した案

| 案 | 採らなかった理由 |
| --- | --- |
| 「対応するノード」の節の中だけを見る | 節を持たない PR が 30 件中 21 件あり、結局「節が無ければ行の頭」の 2 段になる。これまでの PR では結果が行の頭だけの案と同じだった |
| 題を見ない | 題に id を書く慣習がそのまま使えなくなる |

### 取り込む側の作業

**無い。** 通常のマージで済む（`check_refs.py` と PR テンプレートは `merge=ours` ではない）。

---

## 0.10.1 — 2026-09-20

### 修正

**セットアップ手順にラベルの作成を足した。** 0.10.0 の「取り込む側の作業」に
`gh label create` を 3 行書いていたが、**取り込みのたびに読む場所ではない。**
新しく派生を起こした人には届かない。

```bash
gh label clone setopic/graph-project-template
```

`gh` の組み込みで、既にあるラベルは飛ばす。3 行を 1 行にして手順 5 に置いた。

**検出の仕組みは作らない。** ラベルが付いていないことは、**最初の issue を立てた
時点で画面に出る。** 害も無い（issue は正しく立ち、本文に型が書いてある）。
機械で見張る価値が、その仕組みを保つ手間に釣り合わない（META-06 の梯子の 1 段目）。

**手順 3 を `make setup` にした。** 上流の 1.18.0 で入ったもので、
README は `merge=ours` なのでマージでは流れてこない。

---

## 0.10.0 — 2026-09-20

### 追加

**issue の型を 3 つにし、派生から上流へフィードバックする導線を置いた。META-05。**

型は「要件」1 つだけで、`requirement.md` は「決めること」と却下した案を求める形を
していた。**見つかった不具合と、条件待ちの宿題がそこに入らない。** 無理に当てると
「決めること」が空の issue になり、型が嘘になる。

実際、7 リポジトリ合計の issue は 2 件だった。**フローは META-05 にあったが、
入口が 1 つしか無く、使われていなかった。**

| 型 | いつ使うか | 閉じるとき |
| --- | --- | --- |
| **要件**（既存） | 決めることが書ける | 決めることに答えが出たとき |
| **問題**（新） | 現象しか書けない | 原因が分かったとき。**直すのは PR の仕事** |
| **TODO**（新） | いま着手しないが、条件が揃ったらやること | 条件が揃って回し、結論が出たとき |

**3 つとも「まだ答えが無い問い」を持つ。** 分け方は「決めることが書けるか」で足りる。

### テンプレートへのフィードバック

**派生に立てた issue は上流に届かない。** これまで経路が無く、
2026-09-11 の一度きりのメモで運んでいた。**配って初めて分かる欠陥が改善の大半を
占めている**のに、それを拾う常設の道が無かった。

`config.yml` の `contact_links` に上流 2 層への導線を置いた。
**issue の作成画面から選べる。**

| 何の話か | 出し先 |
| --- | --- |
| `tools/graph` と `G0xx` の検査 | `graph-doc-template` |
| 文書と実装の同居・CI・配置・issue の型そのもの | `graph-project-template` |

**GitHub の組み込みの機能だけで済ませた**（META-06 の梯子の 4 段目で止まった）。
ワークフローもスクリプトも足していない。

### 取り込む側の作業

**`git merge template/main` で 3 つの型も META-05 も入る。**

**`merge=ours` は両側が変更したときにだけ働く。** 既定値のまま使っている派生には
新しい「1. issue を立てる」がそのまま流れる。`dev-flow.md` を**手で書き換えている
派生だけ**、自分の版が残るので 3 つの型と上流への出し方を手で足す。

**ラベルの実体はマージでは作られない。** `labels:` に書いた名前のラベルが
リポジトリに無いと、**黙って付かないまま issue が立つ。** 0.10.0 の配布時、
既存の 5 リポジトリすべてで「要件」ラベルが存在せず、`requirement.md` の
`labels: 要件` は最初から効いていなかった。取り込んだら 1 回だけ実行する。

```bash
gh label create "要件" --color 1d76db --description "機能の追加・変更。決めることが書ける"
gh label create "問題" --color d73a4a --description "見つかった不具合。現象しか書けない"
gh label create "TODO" --color fbca04 --description "いま着手しないが、条件が揃ったらやること"
```

---

## 0.9.1 — 2026-09-17

### 修正

**GitHub Project への載せ方の案内を、組み込みの Auto-add を先にする順に直した。**

0.9.0 は `add-to-project.yml`（Actions と PAT）で載せる道しか書いていなかった。
**Project には組み込みの Auto-add があり、トークンもワークフローも要らない。**
0.9.0 で比べた案に、これが入っていなかった。META-06 の梯子でいえば、
4 段目（実行環境の機能で済むか）で止まるべきところを、7 段目まで登っていた。

| 手段 | トークン | 1 つで覆えるリポジトリ | 作れる数 |
| --- | --- | --- | --- |
| **組み込みの Auto-add** | **要らない** | 1 | プランの上限（Free は 1、Pro と Team は 5） |
| `add-to-project.yml` | classic の PAT を秘密に置く | 置いたリポジトリ | 上限なし |

**ワークフローは消していない。** Free のまま複数のリポジトリを 1 つの Project に
まとめると、Auto-add が足りなくなる。そのときの代わりとして残す。
設定しなければ動かないので、置いておく害は無い。

**直したのは案内だけで、ワークフローの動きは変わらない。**

| どこを | どう直したか |
| --- | --- |
| `add-to-project.yml` の冒頭 | Auto-add の設定手順を先に書き、このファイルは数が足りないときに使う、とした。秘密を URL より先に置くことも書いた |
| META-05 の「1. issue を立てる」 | 同じ順にした |
| README の表 | 同じ順にした |

**Auto-add の上限が Project ごとか、持ち主ごとかは、GitHub の文書に書かれていない。**
「プランごとに上限がある」とだけあるので、案内もそこまでしか書いていない。

### 取り込む側の作業

**無い。** 通常のマージで済む。ノードは増えていないので、README の図も変わらない。

---

## 0.9.0 — 2026-09-16

### 追加

**開発の原則を [META-06](docs/00-meta/principles.md) として書き、`/lean` スキルを足した。**

[ponytail](https://github.com/dietrichgebert/ponytail) の決定の梯子
（そもそも要るか → 既にあるか → 標準ライブラリ → 実行環境の機能 → 入っている依存 →
1 行 → 最小実装）を、このテンプレートの語彙で書き直したもの。
**これまで定義されていたのは「何を作るか」を決める `/grill` だけで、
「どれだけ少なく作るか」はどこにも書かれていなかった。**

**`.github/workflows/add-to-project.yml` を足した。** issue と PR を GitHub Project に載せる。
**設定（`PROJECT_URL` と `ADD_TO_PROJECT_PAT`）が無いリポジトリでは何もしない。**

### 決めたこと

**このテンプレートは ADR を起こせないので、ここに残す。** 派生が自分の ADR を
既に持っており（tournament-bot 75 件、medieval-idle 23 件など）、
テンプレートが `ADR-0002` を配ると番号がぶつかる。

| 決めたこと | 採った案 | 却下した案 |
| --- | --- | --- |
| ponytail の取り込み方 | **自己完結で作り直す**（`/grill` と同じ作法） | 本家プラグインを前提にする（入れていない利用者に何も効かない）／同梱する（Node の hooks とスキル 6 本が全派生に流れる） |
| 原則の本体 | **META-06。`CLAUDE.md` とスキルは指すだけ** | `CLAUDE.md` だけ（`check` が届かない）／スキルだけ（呼ばなければ読まれない） |
| 配る層 | **統合型だけ** | 上流に置いて全リポジトリへ（文書だけのリポジトリでは実装の原則が空振りする） |
| 機械で見るもの | **上流の `review` に `A008`・`A009`（文書の過剰な設計）** | `check` にルールを足す（語だけでは過剰と言えず、誤検出が出る）／先送りのコメント規約と台帳 |
| スキルの名前 | **`lean`** | `yagni`（中身より狭い）／`ponytail`（本家と同名で紛らわしい） |
| スキルの担当 | **実装に着手する前と、差分を出す前** | 文書を起こす前／`lite`・`full`・`ultra` のモード（hooks が無いと状態を保てない） |
| Project 連携の範囲 | **issue と PR を載せるだけ** | ノードの `status` を Project に写す（状態がグラフと Project の 2 か所になる）／`gh project` を回すスキル |
| 設定が無いとき | **何もしない** | `if: false` で配る（有効にしたファイルが取り込みで競合する）／配らない |
| 載せるもの | **issue と PR の両方** | issue だけ／`要件` ラベルの issue だけ |

**`review` の観点は上流に入れた。** `review` が読むのは文書ノードの本文だけで、
コードには届かない。しかも `tools/graph` は上流のもので、こちらで書き換えると
取り込みのたびに競合する。**そこで観点を「文書に現れた過剰な設計」に翻訳して
上流へ入れ、コードの側は `/lean` が受け持つ形にした。**

**先送りの印（本家の `ponytail:` コメント）は入れていない。** `CLAUDE.md` に
「コメントには『なぜやらなかったか』を書く」が既にあり、それで足りる。

### 上流

`graph-doc-template` 1.17.0 を取り込んだ（`review` の `A008`・`A009`）。
**META-06 はこれを指している。**

### 取り込む側の作業

**マージしたら README の図を作り直す。** やらないと graph-check が落ちる。

```bash
make readme
```

**ノードが 1 つ増えたためである。** README は `merge=ours` なので派生の図は据え置きになり、
META-06 の分だけ古くなる。

**取り込みを 5 リポジトリで試してから書いた。** 上流 1.17.0 → この層 0.9.0 → 派生、
の順に使い捨てのクローンでマージし、CI と同じ検査を回した。

| リポジトリ | 競合 | テスト・`check --strict`・`sync`・`linkify` | README の図 |
| --- | --- | --- | --- |
| tournament-bot | なし | 通る | **取り込んだ直後は落ちる。`make readme` で通る** |
| medieval-idle | なし | 通る | **同上** |
| gacha-monitor | なし | 通る | **同上** |
| ops-docs（上流 1.17.0 だけ） | なし | 通る | 通る |
| discord-bot-docs（上流 1.17.0 だけ） | なし | 通る | 通る |

**Project を使うときだけ**、リポジトリの
Settings → Secrets and variables → Actions で次の 2 つを足す。手順は
`add-to-project.yml` の冒頭にある。

| 種類 | 名前 | 値 |
| --- | --- | --- |
| Variables | `PROJECT_URL` | `https://github.com/users/<持ち主>/projects/<番号>` |
| Secrets | `ADD_TO_PROJECT_PAT` | classic の PAT。`project` スコープ（非公開リポジトリなら `repo` も） |

**`GITHUB_TOKEN` では Projects v2 に書けない**ので、PAT が要る。

**META-06 は目次（`index.md`）には載らないが、孤立しない。** `index.md` は
`merge=ours` なので、派生の目次は変わらない。**META-04 の `related` から辿れる**
（META-05 と同じ経路）。META-04 は 3 リポジトリともテンプレートと同一なので、
足した 1 行は競合しない。

### `merge=ours` は、派生が書き換えたファイルにだけ効く

**0.8.2 に「`dev-flow.md` は `merge=ours` なので、この直しは流れない」と書いたが、
実際には流れていた。** git がマージドライバを呼ぶのは**両側がそのファイルを変えたとき**
だけで、派生側が触っていなければ上流の版がそのまま入る。

| 派生側 | 上流側 | 取り込んだ結果 |
| --- | --- | --- |
| 触っていない | 変えた | **上流の版が入る**（`merge=ours` は効かない） |
| 変えた | 変えた | 派生の版が残る |

**実験で確かめた。** 使い捨てのリポジトリで両方の場合を作り、上の表どおりになった。
tournament-bot の `dev-flow.md` の履歴にも 0.8.2・0.8.3 のコミットがそのまま入っている。

**したがって今回 META-05 に足した 2 行（Project と `/lean` への案内）も流れる。**
3 リポジトリとも `dev-flow.md` を書き換えておらず、テンプレートと同一である。
**書き換えたプロジェクトには流れない**ので、そのときは自分で足す。

---

## 0.8.8 — 2026-09-12

### 上流

`graph-doc-template` 1.16.5 を取り込んだ。**共有ファイルの記入例の直し。**

1.15.1 の例が字下げのコードブロックに置かれていて、`strip_non_prose` に保護されず、
**`ADR-0012` が実在する派生リポジトリで `linkify --check` を落としていた**
（`medieval-idle` で発生）。囲みをバッククォート 3 つに変えてある。

**共有ファイルに記法の例を書くときは、必ずバッククォートで囲むこと。**

### 取り込む側の作業

**無い。**

---

## 0.8.7 — 2026-09-12

### 上流

`graph-doc-template` 1.16.4 を取り込んだ。**`G020` の設計が定まった版である。**

0.8.4〜0.8.6 は派生へ配りながら 3 回直している。最後のこれが一番大きい。

**`G020` は確定した記録（既定では `stable` な ADR）を見なくなった。**
ADR は確定したら書き換えないもので、決定を変えるときは新しい ADR を起こす。
**古い ADR に要るのは後継へのリンクだけ**なので、本文を直させる指摘は
原理的に解消できない。**確定前（`draft` / `review`）は対象に残る。**

**`CLAUDE.md` に 2 項増えている**（共有ファイルなのでマージで入る）。

- 確定した ADR は不変の記録。古い ADR に要るのは後継へのリンクだけ
- **ADR 以外の層には過去の経緯を書かない。** `10-architecture`〜`40-contracts` は
  「いまどうなっているか」を書く場所で、生きている ADR を指す

この層は引き続き 0 件。

### 取り込む側の作業

**無い。** 警告が減る方向にしか動かない。**上流全体で 41 → 18 ノード。**

---

## 0.8.6 — 2026-09-12

### 上流

`graph-doc-template` 1.16.3 を取り込んだ。**0.8.5 の続きで、`G020` の除外の直し。**

1.16.2 は置き換え先の**連鎖を辿る**ようにしたが、除外のほうが直接の `supersedes`
だけを見たままだったので、`A` → `B` → `C` のとき **`C` が 2 つ前の `A` を引くと
鳴っていた。** 置き換えた側が過去の決定を引くのは仕事のうちなので黙らせた。

**警告が減る方向にしか動かない。** この層は引き続き 0 件。

### 取り込む側の作業

**無い。**

---

## 0.8.5 — 2026-09-12

### 上流

`graph-doc-template` 1.16.2 を取り込んだ。**`G020` が置き換えの連鎖を辿るようになった。**

`ADR-0004` → `ADR-0006` → `ADR-0007` と置き換わったとき、それまでは `ADR-0004` の
置き換え先を `ADR-0006` だけと見ていた。**現在の決定（連鎖の先端）を指すのは、
直接の置き換え先を指すより正しい**ので、両方を認めるようにしたもの。

0.8.4 を派生へ配って使ってみて出た欠陥である。**警告が減る方向にしか動かない。**

この層は引き続き 0 件で通っている。

### 取り込む側の作業

**無い。**

---

## 0.8.4 — 2026-09-12

### 上流

`graph-doc-template` 1.15.0〜1.16.1 を取り込んだ。**検査ルールが 2 つ増えている。**

| 版 | 内容 |
| --- | --- |
| 1.15.0 | `G020` — 取り下げた決定を、断りなく現在の根拠として引いている（**警告**） |
| 1.15.1 | `adr.md` に「置き換えるときは何を引き継ぐかを書く」作法 |
| 1.16.0 | `G021` — 自動生成ブロックより後ろに本文が残っている（**エラー**） |
| 1.16.1 | `review` の `A007`、`architecture.md` と `domain.md` の記入案内 |

**この層は 0 件で通った。** `G020` も `G021` も発火していない。

詳しくは [TEMPLATE_CHANGELOG.md](TEMPLATE_CHANGELOG.md) を読むこと。

### 直したもの

**README のルール表が古くなっていた。** `README.md` は `merge=ours` なので
**上流の更新が伝播しない。** `G020` と `G021` の行が無く、
構成の説明に書いてある範囲も「`G001`〜`G013`」のまま止まっていた
（`G019` を入れた 1.14.0 の時点で既にずれていた）。

上流に合わせて、ルール表の 2 行・重さの説明・`G020` と `G009` の違いを足した。

**取り込むたびに README のずれを見ること。**

```bash
git diff HEAD template/main -- README.md
```

### 取り込む側の作業

**`G020` が出るリポジトリがある。** `--strict` を使っている main では CI が落ちる。

```bash
python -m tools.graph check | grep G020
```

直し方は 2 つ。**どちらかをその段落の中に書く。**

- 置き換え先を指す — `以前は締めだった（[[ADR-0008]]。いまは [[ADR-0014]]）`
- 引き継がれた範囲を明示する — `報告期限は締めではない（[[ADR-0008]] の決定 1 のまま）`

**歴史として引いているだけなら、そのまま放置してよい。**

**README のルール表は各リポジトリで手で合わせる**（`merge=ours` のため）。

---

## 0.8.3 — 2026-09-11

### 直したもの

**0.8.2 で入れた `META-05` の表で、数と行数が合っていなかった。**

「履歴は 3 つに分かれていて、持ち主が違う」と書いた直後の表に
**4 行目（配置の手順）が残っていた。** 配置の手順は履歴ではない。

**これは 0.8.2 の手落ちである。** 上 3 行を足したときに、元からあった
4 行目との関係を書かなかった。

**同じ形の誤りを、tournament-bot の全 160 ノードで機械的に探した。**
「**N つ**」と書いた直後の表の行数・箇条書きの数を突き合わせたところ、
`ADR-0026` と `DOM-10` でも同じずれが見つかっている（あちらで直した）。

```bash
python -m tools.graph check
```

**この検査は `check` に入れていない。** 「N つ」の数え方は文の形に依存し、
**誤検出が本物を上回る**（実測で 15 件中 11 件が誤検出）。
**機械で挙げて人が見る**形のほうが合っている。

### 取り込む側の作業

**無い。** `docs/00-meta/dev-flow.md` は `merge=ours` だが、
**派生側がこのファイルを編集していなければマージで入る**（`merge=ours` は
両側が変更したときにだけ働く）。編集しているなら、同じ 1 行を自分で直す。

---

## 0.8.2 — 2026-09-11

### 上流

`graph-doc-template` 1.14.2 を取り込んだ（本文の AI レビューの指摘。`META-01` の
`G015`〜`G019` を 5 節へ戻し、雛形の一覧に `contract-interaction` を足し、
サンプルノード 4 件を直したもの）。

**取り込みで `graph-rules.md` が競合した。** 上流が 5 節へ移した `G015`〜`G019` が、
こちら側では旧位置に残っていたため。移動後を採り、旧位置の重複を落としている。

### 直したもの

同じレビューで、**この層だけが持つ文書**にも 2 件の指摘が出た。

**`META-05` の「リリースと配置」の表に `PROJECT_TEMPLATE_CHANGELOG.md` が無かった。**
履歴は 3 つに分かれているのに表は 2 つしか載せておらず、**この層の履歴の行き先が
どこにも書かれていなかった。** 実際、1.14.1 を取り込んで 0.8.1 を書いたときも、
行き先は `META-05` からは分からず git の履歴から読んでいる。

**`META-04` の節「要件は issue から始める」が、末尾で自分を否定していた。**
題は「issue から始める」という順序を宣言しているのに、末尾は
「ここが決めているのは『どこに書くか』だけである」と書いていた。
**入口を決めているのは事実なので、末尾のほうを実態に合わせた。**
`CLAUDE.md` の同じ節にはこの一文が無いので、そちらは触っていない。

### 派生プロジェクトに伝えること

**`META-05`（`dev-flow.md`）は `merge=ours` なので、この直しは流れない。**
各プロジェクトの `dev-flow.md` は自分のものである。履歴の行き先で迷ったことが
あるなら、同じ行を自分で足しておくとよい。

**サンプルノードを撤去しているリポジトリでは、上流の取り込みで競合が出る。**
`DOM-01` などが modify/delete として挙がるので、削除側を採る。

---

## 0.8.1 — 2026-09-11

### 上流

`graph-doc-template` 1.14.1 を取り込んだ（雛形の本文をレビューし、
`作成日: {{DATE}}` を 6 つの雛形から外した。派生 7 リポジトリの 198 ノードで
残っていたのは 11 件だけで、運用として成立していなかった）。

あわせて `adr` の日付と状態に記入案内が足され、`contract-interaction` の
「無し」が「なし」に統一され、`usecase` の「概要」表に記入案内が足された。

**この層で足すものは無い。**

### 派生プロジェクトに伝えること

**取り込んでも既存の文書は変わらない。** マージのあと `new` で起こした
ノードに `作成日:` が付かなくなるだけで、既にある `作成日:` を消す必要も無い。

雛形を自分のプロジェクト向けに書き換えている場合だけ競合することがある。
**そのときは自分の側を残してよい。**

---

## 0.8.0 — 2026-09-11

### 上流

`graph-doc-template` 1.14.0 を取り込んだ（`G019`: 表が途中で切れていないかを見る）。

**この層で足すものは無い。**

### 直したもの

**README のルール一覧が `G014` で止まっていた。**`G015`・`G016`・`G017`・`G018`・
`G019` が丸ごと欠けており、**1.6.0（`G015`）から遅れ続けていた。**

`README.md` は `.gitattributes` で `merge=ours` なので、**上流でルールを足しても
伝播しない。** 共有ファイルの `docs/00-meta/graph-rules.md` は正しく伝播しており、
ずれるのは `merge=ours` のものだけである。README がその代表になる。

**同じ取り込みで、README のコマンド表が切れていたのも直した。**1.8.0 で
スキルの案内文を表の行と行の間に挿し込んでおり、**以降の 14 行が本文として
流れていた。** これが `G019` を入れるきっかけになった 2 件のうちの 1 件である。

### 派生プロジェクトに伝えること

**表が切れている文書があれば、取り込んだ時点で main の CI が落ちる。**
落ちるのは正しく、その表は取り込む前から GitHub 上で壊れて表示されている。

```bash
python -m tools.graph check | grep G019
```

**あわせて README のずれも見ておくとよい。** `merge=ours` のファイルは
黙って遅れるので、気づく機会が取り込みのときしかない。

```bash
git diff HEAD template/main -- README.md
```

---

## 0.7.0 — 2026-09-10

### 追加

**開発の流れを [META-05](docs/00-meta/dev-flow.md) として書き下した。**

これまで定義されていたのは**成果物の整合性**（`check`・`G0xx`・`implemented_by`）
だけで、**いつ何をするか**はどこにも書かれていなかった。ブランチの切り方、
コミットの粒度、PR にする条件、レビューの有無、リリースの扱いが全部そうだった。

**`merge=ours` で守っている。** ブランチの切り方もリリースの仕方も
プロジェクトごとに違う。テンプレートが配るのは**既定値**であって規約ではないので、
書き換えたものが取り込みで戻らないようにした。

**`/grill` を回す場面を 4 つに決めた。** これが今回の主眼である。
スキルは 1.8.0 から同梱されていたが、**いつ使うかがどこにも書かれていなかった。**

| いつ | 合図 |
| --- | --- |
| issue を書く前 | 「決めること」が 3 つ以上ある、または埋められない |
| ADR を起こす前 | **却下した案が書けない** |
| 実装に着手する前 | 受け入れ条件が空、または検証できない書き方 |
| `schema.py` の語彙・層を変える前 | 全ノードに波及し、着手してからでは戻せない |

**強制はしない。** grill を回したかは CI から見えないので、
表には「回さないと何が起きるか」まで書いてある。**代償を先に見せる形にした。**

**`CONTRIBUTING.md` を足した。** GitHub が issue / PR の作成画面に自動で
リンクするための入口で、**META-05 を指すだけで中身は持たない。**
`merge=ours` にしていないのは、指し先のパスが全プロジェクトで同じで、
プロジェクト側が書き換える理由が無いため。

### 実態から書いた

**PR もタグも全リポジトリで 0 件だった。** ブランチは `main` だけ。
つまり PR テンプレートと `app-check.yml` の `refs` ジョブは**一度も動いていない。**

そこで「全部 PR にする」とは書かず、**PR のときだけ動く検査 2 つ**を挙げて、
それが要る変更だけを PR にすると決めた。

| 検査 | 直接 push だと |
| --- | --- |
| `check --since origin/<base>` | `G015` の窓が広がらない。**コミットすると見えなくなる** |
| `app-check.yml` の `refs` | 走らない |

**PR を全部に強制すると、この 2 つが「いつもの儀式」になって読まれなくなる。**

### 上流

`graph-doc-template` 1.13.0 を取り込んだ（`/grill` の差分表・調査の非ブロッキング規則・
起動する場面の追加）。

### 取り込む側の作業

なし。通常のマージで済む。

**ただし META-05 は既定値なので、自分のやり方と違うなら書き換える。**
書き換えても取り込みで戻らない。変えたことは `PROJECT_CHANGELOG.md` に書く。

---

## 0.6.0 — 2026-09-10

### 上流

`graph-doc-template` 1.12.0 を取り込んだ（サンプルの改名と README の整合）。

**この層で足すものは無い。**

**派生プロジェクトに伝えること。** サンプルと同じファイル名を自分のノードに
使っている場合は、**取り込む前に改名する。** そのままだと修正/削除の競合になり、
サンプルの中身が自分の文書に混ざる。

```bash
python -m tools.graph rename --from ARCH-01 --slug <このプロジェクトらしい名前>
```

## 0.5.0 — 2026-09-10

### 上流

`graph-doc-template` 1.11.0 を取り込んだ（`linkify`）。

**この層で足すものは無い。**

**派生プロジェクトに伝えること。** 1 回だけ `python -m tools.graph linkify` を
回してコミットする。回すまで CI が落ちる。グラフは変わらないので `check` の
結果は前後で一致するが、**差分は大きくなる**（本文の `[[ID]]` が全部
相対リンクに変わるため）。

## 0.4.0 — 2026-09-10

**履歴のファイル名を層ごとに分けた。**

### 何が起きていたか

このファイルはもともと `PROJECT_CHANGELOG.md` という名前だった。
ところが**派生プロジェクトも同じ名前で自分の履歴を書こうとする。**
名前が 1 つで所有者が 2 人いる状態で、取り込むたびに派生側が上書きされる。

実際、統合型の派生 2 件（`tournament-docs` / `medieval-idle-docs`）は
**自分の履歴を 1 行も持たないまま、この層の履歴をそのまま抱えていた。**

### 決めたこと

- この層の履歴を `PROJECT_TEMPLATE_CHANGELOG.md` に改名した
- `PROJECT_CHANGELOG.md` は**プロジェクト自身のもの**とし、雛形だけを配る
- 上流（1.10.0）の `.gitattributes` が `PROJECT_CHANGELOG.md merge=ours` を持つ

### 取り込む側の作業

**`PROJECT_CHANGELOG.md` に、この層の履歴（「graph-project-template の変更履歴」
で始まるもの）が入っているなら、中身を自分の履歴に書き換える。**
以後は上書きされない。この層の履歴は `PROJECT_TEMPLATE_CHANGELOG.md` で読む。

---

## 0.3.0 — 2026-09-10

### 上流

`graph-doc-template` 1.9.0 を取り込んだ（`G018` と `render --aggregate`）。

**この層で足すものは無い。** 統合型に固有の事情は無く、文書だけの型と同じように効く。

**派生プロジェクトに伝えること。** `G018` は新しいエラーなので、README の図が
既に GitHub の描画上限（エッジ 500 本）に達しているリポジトリは、取り込んだ時点で
main の CI が落ちる。落ちるのは正しく、その README の図は**取り込む前から
GitHub 上で描画されていない**。直し方は `TEMPLATE_CHANGELOG.md` の 1.9.0 にある。

## 0.2.0 — 2026-09-08

### 追加

**`CLAUDE.md` に「実装を書くときの決まり」を足した。**

- **モジュールの説明の 1 行目に、規定しているノードの id を書く。**
  これが `implemented_by` の根拠になる。実プロジェクトではこの 1 行のおかげで
  対応を推測せずに起こせた（tournament 95% / medieval-idle 100%）
- **識別子はドメインの正式な用語を使う。**`G013` はコードに届かないので、
  機械では確かめられない。書くときに合わせる
- **コメントには「なぜやらなかったか」を書く。**
  長くなるならそれは ADR に書くべきもの

記事（[keitakn/engineering-skills](https://github.com/keitakn/engineering-skills)）の
`code-naming` / `code-comments` に相当するものだが、**汎用のガイドは入れていない。**
このプロジェクトには用語表という具体的な正解があり、汎用の命名論より強い。

### 上流

`graph-doc-template` 1.8.0 を取り込んだ（`/grill` スキル）。

---

## 0.1.1 — 2026-09-07

### 直したもの

- **`CLAUDE.md` の `implemented_by` の例が根からのパスだと読めなかった。**
  `- tournament/teams.py` とだけ書いてあり、**どこからのパスかを言っていなかった。**
  実装を `app/` の下に置いた派生（tournament / medieval-idle のどちらもそうしている）で
  そのまま真似すると、指し先が無いので **`G016` がエラーになる。**
  例を `app/tournament/teams.py` に変え、**根からのパスであること**と、
  **先頭のディレクトリ名はテンプレートが決めていないこと**（[META-04](docs/00-meta/implementation-layout.md)）を書き足した。
  **`app/` を規約にしたわけではない。**0.1.0 の「足さなかったもの」のとおり、
  実装のディレクトリ名は自由なままである

### 変えていないもの

- **`docs/00-meta/graph-rules.md` の同じ例。**あちらは上流（`graph-doc-template`）と
  共通のファイルで、**文書だけの型には `app/` が無い。**
  こちらで直すと次の `git merge template/main` で競合する

---

## 0.1.0 — 2026-09-07

最初の版。`graph-doc-template` 1.7.0 から起こした。

### 足したもの

- **[META-04](docs/00-meta/implementation-layout.md)** — 文書と実装を同じリポジトリに
  置く理由、置き方、対応の粒度、既にある実装の合流手順
- **`.github/workflows/app-check.yml`** — 実装の検査と、PR が指すノードが実在するかの確認。
  `graph-check.yml` とは別ファイルにして `merge=ours` で守っている。
  **実装のテストを `graph-check.yml` に足すと、テンプレートを取り込むたびに競合する**
- **`.github/scripts/check_refs.py`** — PR 本文のノード id を解決する。
  `なし` と書けば通す
- **`.github/ISSUE_TEMPLATE/requirement.md`** と **`pull_request_template.md`**
- **`.gitignore`** — `.env` と実行時にできるものを除外
- **`CLAUDE.md`** に「文書と実装が同じリポジトリにあるとき」の節

### 足さなかったもの

- **実装計画のファイル。** 受け入れ条件はユースケースが、決定は ADR が持っている。
  3 つ目の文書種別を作ると、同じことを 2 箇所に書くことになる
- **実装のディレクトリ名の規約。** `src/` でも `tournament/` でもよい。
  予約しているのは `docs/` と `tools/graph/` だけ
- **`tools/` への変更。** `implemented_by` と `G016` / `G017` は上流
  （`graph-doc-template` 1.7.0）に入れてある。こちらで書き換えると、
  以後の取り込みで `rules.py` と `schema.py` が競合する
